# Docs

Place screenshots here so they render in the project README:

- `docs/ui_main.png` — Main realtime UI window
- `docs/plot_example.png` — Example speed/current plot

Tip: On Windows, you can capture images with `Win + Shift + S` and save them under `docs/`.
